<%- include ("../../partials/header.ejs") %>
<%- include ("../../partials/navbar.ejs") %>

<div class="container">
    <hr>
    <div class="card">
        <div class="card-header">
            <h2>Editar Artigo</h2>
        </div>
        <div class="card-body">
            <form method="POST" action="/imoveis/update">
                <!-- Campo oculto com o ID do artigo -->
                <input type="hidden" name="id" value="<%= imoveis.id %>">

                <!-- Título do artigo -->
                <input class="form-control" type="text" name="title" placeholder="Defina o Título do Artigo" value="<%= imoveis.title %>"><br>

                <!-- Corpo do artigo com conteúdo já inserido -->
                <textarea class="form-control" name="body" id="imoveis" placeholder="Escreva o conteúdo aqui!"><%= imoveis.body %></textarea><br>

                <!-- Campo para URL da imagem -->
                <input class="form-control" type="text" name="imageUrl" placeholder="URL da Imagem" value="<%= imoveis.imageUrl %>"><br>

                <!-- Seleção de categoria com a atual pré-selecionada -->
                <label>Categoria</label>
                <select name="category" class="form-control">
                    <% categories.forEach((category) => { %>
                        <option value="<%= category.id %>" <%= category.id === imoveis.categoryId ? "selected" : "" %>>
                            <%= category.title %>
                        </option>
                    <% }) %>
                </select><br>

                <!-- Botão de envio -->
                <button class="btn btn-success">Atualizar</button>
            </form>
        </div>
    </div>
</div>

<%- include ("../../partials/footer.ejs") %>

<!-- TinyMCE -->
<script src="/tinymce/tinymce.min.js"></script>
<script>
    tinymce.init({
        language: 'pt_BR',
        selector: "#imoveis",
        plugins: [
            'advlist autolink link image lists print preview hr searchreplace wordcount fullscreen insertdatetime media save table paste emoticons'
        ],
        toolbar:
            'undo redo | formatselect | bold italic backcolor | \
            alignleft aligncenter alignright alignjustify | \
            bullist numlist outdent indent | removeformat | help'
    });
</script>
